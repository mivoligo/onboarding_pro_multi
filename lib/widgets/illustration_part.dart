import 'package:flutter/material.dart';

class IllustrationPart extends StatelessWidget {
  const IllustrationPart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
